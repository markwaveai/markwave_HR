{
    "name": "keka-markwave-monorepo",
    "private": true,
    "scripts": {
        "web": "cd web && npm run dev",
        "mobile": "cd mobile && npm run start",
        "shared:build": "cd shared && npm run build"
    },
    "devDependencies": {
        "@react-native/metro-config": "^0.83.1"
    }
}